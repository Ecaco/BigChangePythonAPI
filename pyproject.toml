[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bigchange"
version = "0.0.1"
dependencies = ["httpx"]

[tool.hatch.build.targets.wheel]
packages = ["src/bigchange"]